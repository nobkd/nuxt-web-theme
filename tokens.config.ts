import { defineTheme, palette } from 'pinceau';

export default defineTheme({
    color: {
        primary: palette('#00DC82'),
    },
    prose: {
        h1: {
            margin: '1rem 0 2rem',
        },
    },
});
